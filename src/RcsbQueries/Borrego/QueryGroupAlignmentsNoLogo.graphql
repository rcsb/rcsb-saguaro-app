query QueryGroupAlignmentNoLogo($group: GroupReference!, $groupId: String!, $filter: [String!]){
    group_alignment(
        group: $group
        groupId: $groupId
        filter: $filter
    ){
        query_sequence

    }
}